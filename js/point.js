/**
 * @property {int} y    - point row
 * @property {int} x    - point column
 */
class Point {
    /**
     * @param {int} y
     * @param {int} x
     */
    constructor(y, x) {
        this.y = y;
        this.x = x;
    }
}
